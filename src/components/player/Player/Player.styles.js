import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    backgroundColor: "#000",
    color: "#fff",
    height: 250,
    width: "100%",
  },
  video: {
    width: "100%",
    height: "100%",
  },
});
